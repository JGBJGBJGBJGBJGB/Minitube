apply plugin: 'com.android.application'

android {
    compileSdkVersion 16

    defaultConfig {
        applicationId "com.example.miniapk"
        minSdkVersion 16
        targetSdkVersion 16
        versionCode 1
        versionName "1.0"
    }

    buildTypes {
        release {
            minifyEnabled false
        }
    }
}

dependencies {
}
